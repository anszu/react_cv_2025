import { DataProvider } from 'src/Data/DataProvider';

export const App = () => {
    return (
        <DataProvider>
            <h1 className="text-3xl font-bold underline">CV App</h1>
        </DataProvider>
    );
};
